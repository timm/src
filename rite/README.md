# Explanation of multi-objective reasoning in software engineering, 2021-2026

goal:  explanation of multi-objective reasoning in software engineering
years: 2021-2026

The `goal:` and `years:` lines above are machine-read by
etc/fetch.py (and, through it, etc/snowball.py). They are
the single source of truth for the literature search; to
retarget this repo, edit them and etc/flags.py, then rerun
the pipeline (see CLAUDE.md).
