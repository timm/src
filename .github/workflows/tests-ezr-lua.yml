name: ezr-lua
on:
  push:
    branches: [main]
    paths:
      - "ezr-lua/**"
      - ".github/workflows/tests-ezr-lua.yml"
  workflow_dispatch:
jobs:
  eg:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: timm/moot
          path: moot
      - run: |
          sudo apt-get update -q
          sudo apt-get install -y lua5.4
          sudo ln -sf /usr/bin/lua5.4 /usr/local/bin/lua
      - run: cd ezr-lua && lua xai-eg.lua --all
        env:
          MOOT: ${{ github.workspace }}/moot
      - run: cd ezr-lua && lua xai-eg.lua --join
      - run: cd ezr-lua && lua xai-eg.lua --check
        env:
          MOOT: ${{ github.workspace }}/moot
      - run: cd ezr-lua && lua xaiplus-eg.lua --all
        env:
          MOOT: ${{ github.workspace }}/moot
      - run: cd ezr-lua && lua xaiplus-eg.lua --join
      - run: cd ezr-lua && lua xaiplus-eg.lua --check
        env:
          MOOT: ${{ github.workspace }}/moot
